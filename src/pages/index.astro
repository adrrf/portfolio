---
import { getCollection } from "astro:content";
import Container from "@components/Container.astro";
import PageLayout from "@layouts/PageLayout.astro";
import ArrowCard from "@components/ArrowCard.astro";
import Link from "@components/Link.astro";
import { dateRange } from "@lib/utils";
import { SITE, HOME, SOCIALS } from "@consts";
import { Image } from "astro:assets";

const blog = (await getCollection("blog"))
  .filter(post => !post.data.draft)
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0,SITE.NUM_POSTS_ON_HOMEPAGE);

const projects = (await getCollection("projects"))
  .filter(project => !project.data.draft)
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0,SITE.NUM_PROJECTS_ON_HOMEPAGE);

const allwork = (await getCollection("work"))
  .sort((a, b) => new Date(b.data.dateStart).valueOf() - new Date(a.data.dateStart).valueOf())
  .slice(0,SITE.NUM_WORKS_ON_HOMEPAGE);

const work = await Promise.all(
  allwork.map(async (item) => {
    const { Content } = await item.render();
    return { ...item, Content };
  })
);

const skills = (await getCollection("skills"));

---

<PageLayout title={HOME.TITLE} description={HOME.DESCRIPTION}>
  <Container>
    <div class="flex items-center animate space-x-4">

      <Fragment>
        <Image src="/adrian.png" alt="Adrián" width="48" height="48" class="w-48 h-48 rounded-full" />
      </Fragment>
      <h1 class="animate text-3xl font-semibold text-black dark:text-white">
        Hi, I'm Adrián <span class="text-3xl">👋🏽</span> 
      </h1>
    </div>
    </div>
    <div class="space-y-16">
      <section>
        <article class="space-y-4">
            <p class="animate font-sans">
              I am a software engineer based in Seville, Spain. I am passionate about technology, software development, 
              and science. My main focus is to continuously expand my technical and theoretical knowledge while gaining 
              practical experience in the field. I am always eager to learn new skills and thrive in collaborative team 
              environments.
            </p>
          <p class="animate">
            
          </p>
          <p class="animate">
            I am dedicated to advancing my career in software engineering and contributing to innovative projects that 
            make a meaningful impact. If you have any inquiries or would like to connect, feel free to reach out via 
            <Link href={`mailto:${SITE.EMAIL}`} aria-label={`Email ${SITE.NAME}`}>
              email
            </Link> 
            or through my 
            <Link href={SOCIALS[1].HREF} external aria-label={`Me on ${SOCIALS[1].NAME}`}>
              LinkedIn profile.
            </Link>
          </p>
          <p class="animate">
            
          </p>
          <p class="animate">
            Main interests: 
            <p class="text-sm opacity-75 text-center animate">
              Backend Development, DevOps, Quantum Computing, Cloud Computing and Machine Learning.
          </p>
        </article>
      </section>

      <section class="animate space-y-6">
        <div class="flex flex-wrap gap-y-2 items-center justify-between">
          <h5 class="font-semibold text-black dark:text-white">
            Work Experience
          </h5>
          <Link href="/work">
            See all work
          </Link>
        </div>
        <ul class="flex flex-col space-y-4">
          {work.map(entry => (
            <li>
              <div class="text-sm opacity-75">
                {dateRange(entry.data.dateStart, entry.data.dateEnd)}
              </div>
              <div class="font-semibold text-black dark:text-white">
                {entry.data.company} - {entry.data.location}
              </div>
              <div class="text-sm opacity-75">
                {entry.data.role}
              </div>
              <article>
                <entry.Content />
              </article>
            </li>
          ))}
        </ul>
      </section>

      <section class="animate space-y-6">
        <div class="flex flex-wrap gap-y-2 items-center justify-between">
          <h5 class="font-semibold text-black dark:text-white">
            Skills
          </h5>
        </div>
        <ul class="flex-col">
          {skills.map(skill => (
           <article>
            {skill.data.type}
            <li class="text-sm opacity-75">
              {skill.data.list.join(", ")}.
            </li>
            </article>
          ))}
        </ul>
      </section>

      <section class="animate space-y-6">
        <div class="flex flex-wrap gap-y-2 items-center justify-between">
          <h5 class="font-semibold text-black dark:text-white">
            Recent projects
          </h5>
          <Link href="/projects">
            See all projects
          </Link>
        </div>
        {projects.length === 0 && (
          <p class="text-sm opacity-75 text-center">
            No projects yet 😔 Check back 🔜!
          </p>
        )}
        <ul class="flex flex-col gap-4">
          {projects.map(project => (
            <li>
              <ArrowCard entry={project} />
            </li>
          ))}
        </ul>
      </section>

      <section class="animate space-y-6">
        <div class="flex flex-wrap gap-y-2 items-center justify-between">
          <h5 class="font-semibold text-black dark:text-white">
            Latest posts
          </h5>
          <Link href="/blog">
            See all posts
          </Link>
        </div>
        {blog.length === 0 && (
          <p class="text-sm opacity-75 text-center">
            No posts yet 😔 Check back 🔜!
          </p>
        )}
        <ul class="flex flex-col gap-4">
          {blog.map(post => (
            <li>
              <ArrowCard entry={post} />
            </li>
          ))}
        </ul>
      </section>

      <section class="animate space-y-4">
        <h5 class="font-semibold text-black dark:text-white">
          Let's Connect
        </h5>
        <article>
          <p>
            If you want to get in touch with me about something or just to say hi,
            reach out on social media or send me an email.
          </p>
        </article>
        <ul class="flex flex-wrap gap-2">
          {SOCIALS.map(SOCIAL => (
            <li class="flex gap-x-2 text-nowrap">
              <Link href={SOCIAL.HREF} external aria-label={`${SITE.NAME} on ${SOCIAL.NAME}`}>
                {SOCIAL.NAME}
              </Link>
              {"/"}
            </li>
          ))}
          <li class="line-clamp-1">
            <Link href={`mailto:${SITE.EMAIL}`} aria-label={`Email ${SITE.NAME}`}>
              {SITE.EMAIL}
            </Link>
          </li>
        </ul>
      </section>
    </div>
  </Container>
</PageLayout>
